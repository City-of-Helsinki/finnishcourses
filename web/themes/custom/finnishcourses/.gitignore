# Ignore the node modules folder (created by 'npm install').
node_modules/*

# Ignore bower libraries 
# (It is suggested to not commit that one to your VCS (just as with the node_modules directory).)
# libraries


# We absolutely don't want to have the .sass-cache in git.
.sass-cache
